[project]
name = "agent"
version = "0.1.0"
description = "Retrieve information from my library (PDF books)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "uvicorn[standard]>=0.34.3",
]
